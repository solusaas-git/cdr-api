# CDR API Architecture

Visual guide to understand how the PostgreSQL replica solution works.

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         PRODUCTION ENVIRONMENT                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                    Sippy Softswitch Server                    │  │
│  │                        (FreeBSD)                              │  │
│  ├──────────────────────────────────────────────────────────────┤  │
│  │                                                               │  │
│  │  ┌─────────────────────────────────────────────────────┐    │  │
│  │  │         PostgreSQL Master Database                   │    │  │
│  │  │              (Production CDR Data)                   │    │  │
│  │  │                                                       │    │  │
│  │  │  • Handles live call processing                      │    │  │
│  │  │  • Writes CDRs in real-time                         │    │  │
│  │  │  • Serves Sippy XML API                             │    │  │
│  │  │  • Streams WAL to replica ─────────────────┐        │    │  │
│  │  └─────────────────────────────────────────────┘        │    │  │
│  │                                                          │    │  │
│  │  ┌─────────────────────────────────────────────────────┐│    │  │
│  │  │              Sippy XML API                           ││    │  │
│  │  │         (Legacy, Slow, XML-based)                    ││    │  │
│  │  └─────────────────────────────────────────────────────┘│    │  │
│  └──────────────────────────────────────────────────────────┘    │  │
│                                                          │         │  │
└──────────────────────────────────────────────────────────┼─────────┘
                                                           │
                                                           │ WAL Streaming
                                                           │ (Real-time)
                                                           │ Port: 5432
                                                           │
┌──────────────────────────────────────────────────────────▼─────────┐
│                      REPLICA ENVIRONMENT                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │                    Replica Server                             │ │
│  │                  (Ubuntu/Debian)                              │ │
│  ├──────────────────────────────────────────────────────────────┤ │
│  │                                                               │ │
│  │  ┌─────────────────────────────────────────────────────┐    │ │
│  │  │      PostgreSQL Replica Database (Read-Only)        │    │ │
│  │  │                                                       │    │ │
│  │  │  • Receives WAL stream from master                  │    │ │
│  │  │  • Applies changes in real-time                     │    │ │
│  │  │  • Read-only queries (no writes)                    │    │ │
│  │  │  • Lag: < 1 second                                  │    │ │
│  │  │  • Optimized indexes for CDR queries                │    │ │
│  │  └─────────────────────────────────────────────────────┘    │ │
│  │                            ▲                                  │ │
│  │                            │                                  │ │
│  │                            │ Direct SQL Queries               │ │
│  │                            │ (Connection Pool)                │ │
│  │                            │                                  │ │
│  │  ┌─────────────────────────────────────────────────────┐    │ │
│  │  │         Custom CDR API (Node.js + Fastify)          │    │ │
│  │  │                   Port: 3001                         │    │ │
│  │  │                                                       │    │ │
│  │  │  • High-performance REST API                        │    │ │
│  │  │  • Connection pooling (20 connections)              │    │ │
│  │  │  • Handles 100K+ records per request                │    │ │
│  │  │  • 10-50x faster than XML API                       │    │ │
│  │  │  • JSON responses                                   │    │ │
│  │  │  • Batch processing support                         │    │ │
│  │  └─────────────────────────────────────────────────────┘    │ │
│  │                            ▲                                  │ │
│  │                            │                                  │ │
│  │  ┌─────────────────────────────────────────────────────┐    │ │
│  │  │         Nginx Reverse Proxy (Optional)              │    │ │
│  │  │              Port: 443 (HTTPS)                       │    │ │
│  │  │                                                       │    │ │
│  │  │  • SSL/TLS termination                              │    │ │
│  │  │  • Rate limiting                                     │    │ │
│  │  │  • Load balancing                                    │    │ │
│  │  └─────────────────────────────────────────────────────┘    │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                            ▲                                       │
└────────────────────────────┼───────────────────────────────────────┘
                             │
                             │ HTTPS/HTTP
                             │ JSON API Calls
                             │
┌────────────────────────────▼───────────────────────────────────────┐
│                    YOUR APPLICATION SERVER                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │                  Next.js Application                          │ │
│  │                    (Your OVO App)                             │ │
│  ├──────────────────────────────────────────────────────────────┤ │
│  │                                                               │ │
│  │  ┌─────────────────────────────────────────────────────┐    │ │
│  │  │         Next.js API Routes                          │    │ │
│  │  │                                                       │    │ │
│  │  │  /api/sippy/account/[id]/cdrs                       │    │ │
│  │  │  /api/admin/cdrs                                    │    │ │
│  │  │                                                       │    │ │
│  │  │  • Calls CDR API instead of Sippy XML API          │    │ │
│  │  │  • Enriches with carrier info (MongoDB)            │    │ │
│  │  │  • Returns to frontend                              │    │ │
│  │  └─────────────────────────────────────────────────────┘    │ │
│  │                            ▲                                  │ │
│  │                            │                                  │ │
│  │  ┌─────────────────────────────────────────────────────┐    │ │
│  │  │         Frontend Components                          │    │ │
│  │  │                                                       │    │ │
│  │  │  • CdrReports.tsx (User view)                       │    │ │
│  │  │  • AdminCdrReports.tsx (Admin view)                 │    │ │
│  │  │                                                       │    │ │
│  │  │  • Displays CDR data                                │    │ │
│  │  │  • Shows carrier info                               │    │ │
│  │  │  • Summary statistics                               │    │ │
│  │  └─────────────────────────────────────────────────────┘    │ │
│  │                                                               │ │
│  │  ┌─────────────────────────────────────────────────────┐    │ │
│  │  │         MongoDB (Carrier Data)                       │    │ │
│  │  │                                                       │    │ │
│  │  │  • ARCEP numbering resources                        │    │ │
│  │  │  • Carrier identifiers                              │    │ │
│  │  │  • Used for carrier lookup                          │    │ │
│  │  └─────────────────────────────────────────────────────┘    │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow

### Request Flow (New Architecture)

```
User Browser
    │
    │ 1. User requests CDR data
    ▼
┌─────────────────────┐
│  Next.js Frontend   │
│  (CdrReports.tsx)   │
└──────────┬──────────┘
           │ 2. Fetch from API route
           ▼
┌─────────────────────┐
│  Next.js API Route  │
│  /api/.../cdrs      │
└──────────┬──────────┘
           │ 3. Call CDR API
           ▼
┌─────────────────────┐
│   CDR API Service   │
│ (cdrApiService.ts)  │
└──────────┬──────────┘
           │ 4. HTTP request to CDR API
           ▼
┌─────────────────────┐
│   CDR API Server    │
│  (Fastify/Node.js)  │
└──────────┬──────────┘
           │ 5. SQL query
           ▼
┌─────────────────────┐
│ PostgreSQL Replica  │
│   (Read-only DB)    │
└──────────┬──────────┘
           │ 6. Return CDR rows
           ▼
┌─────────────────────┐
│   CDR API Server    │
│  (Format as JSON)   │
└──────────┬──────────┘
           │ 7. JSON response
           ▼
┌─────────────────────┐
│  Next.js API Route  │
│ (Enrich with carrier│
│  info from MongoDB) │
└──────────┬──────────┘
           │ 8. Return enriched data
           ▼
┌─────────────────────┐
│  Next.js Frontend   │
│  (Display in table) │
└──────────┬──────────┘
           │ 9. Render to user
           ▼
      User Browser
```

### Timing Breakdown (Example: 100K records)

```
Total Time: ~25 seconds

┌─────────────────────────────────────────────────────────┐
│ Step                          │ Time      │ Percentage  │
├───────────────────────────────┼───────────┼─────────────┤
│ 1. Browser → Next.js          │ ~50ms     │ 0.2%        │
│ 2. Next.js → CDR API          │ ~100ms    │ 0.4%        │
│ 3. CDR API → PostgreSQL       │ ~20s      │ 80%         │
│ 4. PostgreSQL → CDR API       │ ~2s       │ 8%          │
│ 5. CDR API → Next.js          │ ~500ms    │ 2%          │
│ 6. Carrier lookup (MongoDB)   │ ~600ms    │ 2.4%        │
│ 7. Next.js → Browser          │ ~1.5s     │ 6%          │
│ 8. Browser rendering          │ ~250ms    │ 1%          │
└───────────────────────────────┴───────────┴─────────────┘

Bottleneck: PostgreSQL query execution (80%)
Optimization: Indexes, query tuning, parallel processing
```

---

## 🔄 Replication Process

### How PostgreSQL Streaming Replication Works

```
Master (Sippy)                           Replica (Your Server)
─────────────                            ──────────────────────

1. INSERT INTO xdrs ...
   │
   │ Write to WAL
   ▼
┌─────────────────┐
│  WAL Segment    │
│  (Binary log)   │
└────────┬────────┘
         │
         │ Stream over network
         │ (Continuous, real-time)
         │
         ▼
      ┌─────────────────┐
      │  WAL Receiver   │
      │  (Replica)      │
      └────────┬────────┘
               │
               │ Write to local WAL
               ▼
      ┌─────────────────┐
      │  WAL Segment    │
      │  (Replica copy) │
      └────────┬────────┘
               │
               │ Apply changes
               ▼
      ┌─────────────────┐
      │  Replica DB     │
      │  (Updated)      │
      └─────────────────┘

Lag: Typically < 1 second
```

### Replication Lag Factors

```
Factors affecting lag:

1. Network latency
   ├─ Local network: < 1ms
   ├─ Same datacenter: 1-5ms
   └─ Different regions: 50-200ms

2. Master write load
   ├─ Low (< 100 TPS): Minimal lag
   ├─ Medium (100-1000 TPS): < 1s lag
   └─ High (> 1000 TPS): 1-5s lag

3. Replica hardware
   ├─ Fast SSD: Minimal impact
   ├─ Slow HDD: Can add 1-2s lag
   └─ CPU: Usually not a bottleneck

4. Network bandwidth
   ├─ 1 Gbps: Sufficient for most cases
   └─ 100 Mbps: May cause lag under heavy load
```

---

## 📊 Performance Comparison

### Old Architecture (Sippy XML API)

```
User Request
    │
    ▼
Next.js API
    │
    │ XML-RPC over HTTP
    ▼
Sippy Server
    │
    │ Parse XML
    │ Query production DB
    │ Generate XML response
    │ (Heavy processing)
    ▼
Next.js API
    │
    │ Parse XML
    │ Convert to JSON
    ▼
Frontend

Problems:
❌ Slow (30-300 seconds for large datasets)
❌ XML parsing overhead
❌ Limited to small datasets (< 50K)
❌ Loads production server
❌ Timeouts on large queries
❌ No batch processing
```

### New Architecture (CDR API + Replica)

```
User Request
    │
    ▼
Next.js API
    │
    │ JSON over HTTP
    ▼
CDR API
    │
    │ Direct SQL query
    │ Connection pooling
    │ Optimized indexes
    ▼
Replica DB (Read-only)
    │
    │ Fast query execution
    │ No production impact
    ▼
CDR API
    │
    │ JSON response
    ▼
Next.js API
    │
    │ Carrier enrichment
    ▼
Frontend

Benefits:
✅ Fast (2-30 seconds for large datasets)
✅ Native JSON
✅ Handles 100K+ records
✅ Zero production load
✅ Reliable (no timeouts)
✅ Batch processing support
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    Security Layers                       │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  Layer 1: Network Firewall                              │
│  ┌─────────────────────────────────────────────────┐   │
│  │ • Only allow specific IPs                        │   │
│  │ • Block all other traffic                        │   │
│  │ • Rate limiting at network level                 │   │
│  └─────────────────────────────────────────────────┘   │
│                         ▼                                │
│  Layer 2: Nginx (Reverse Proxy)                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ • SSL/TLS termination                            │   │
│  │ • Rate limiting (10 req/s per IP)                │   │
│  │ • Request size limits                            │   │
│  │ • DDoS protection                                │   │
│  └─────────────────────────────────────────────────┘   │
│                         ▼                                │
│  Layer 3: API Authentication                             │
│  ┌─────────────────────────────────────────────────┐   │
│  │ • JWT validation                                 │   │
│  │ • API key verification                           │   │
│  │ • Session management                             │   │
│  └─────────────────────────────────────────────────┘   │
│                         ▼                                │
│  Layer 4: Database Access Control                        │
│  ┌─────────────────────────────────────────────────┐   │
│  │ • Read-only user (replicator)                    │   │
│  │ • Limited to SELECT queries                      │   │
│  │ • Connection pooling (max 20)                    │   │
│  │ • Query timeout (10 minutes)                     │   │
│  └─────────────────────────────────────────────────┘   │
│                         ▼                                │
│  Layer 5: PostgreSQL Security                            │
│  ┌─────────────────────────────────────────────────┐   │
│  │ • Listen on localhost only                       │   │
│  │ • Strong password (SCRAM-SHA-256)                │   │
│  │ • Row-level security (optional)                  │   │
│  │ • Audit logging                                  │   │
│  └─────────────────────────────────────────────────┘   │
│                                                           │
└─────────────────────────────────────────────────────────┘
```

---

## 📈 Scalability

### Horizontal Scaling

```
                    ┌─────────────────┐
                    │  Load Balancer  │
                    │    (Nginx)      │
                    └────────┬────────┘
                             │
              ┌──────────────┼──────────────┐
              │              │              │
              ▼              ▼              ▼
       ┌──────────┐   ┌──────────┐   ┌──────────┐
       │ CDR API  │   │ CDR API  │   │ CDR API  │
       │Instance 1│   │Instance 2│   │Instance 3│
       └────┬─────┘   └────┬─────┘   └────┬─────┘
            │              │              │
            └──────────────┼──────────────┘
                           │
                           ▼
                  ┌─────────────────┐
                  │ PostgreSQL      │
                  │   Replica       │
                  └─────────────────┘

Benefits:
• Handle more concurrent requests
• Better fault tolerance
• Rolling updates without downtime
```

### Multiple Replicas

```
         ┌─────────────────┐
         │  Master (Sippy) │
         └────────┬────────┘
                  │
         ┌────────┼────────┐
         │        │        │
         ▼        ▼        ▼
    ┌────────┐┌────────┐┌────────┐
    │Replica1││Replica2││Replica3│
    └───┬────┘└───┬────┘└───┬────┘
        │         │         │
        ▼         ▼         ▼
    ┌────────┐┌────────┐┌────────┐
    │ API 1  ││ API 2  ││ API 3  │
    └────────┘└────────┘└────────┘

Benefits:
• Geographic distribution
• Disaster recovery
• Read load distribution
```

---

## 🎯 Best Practices

### 1. Monitoring

```
┌─────────────────────────────────────────┐
│           Monitoring Stack               │
├─────────────────────────────────────────┤
│                                           │
│  PostgreSQL Metrics:                     │
│  • Replication lag                       │
│  • Query performance                     │
│  • Connection count                      │
│  • Disk usage                            │
│                                           │
│  API Metrics:                            │
│  • Request rate                          │
│  • Response time                         │
│  • Error rate                            │
│  • Memory/CPU usage                      │
│                                           │
│  Alerts:                                 │
│  • Lag > 60 seconds                      │
│  • API down                              │
│  • Disk > 80% full                       │
│  • Error rate > 1%                       │
│                                           │
└─────────────────────────────────────────┘
```

### 2. Backup Strategy

```
Master (Sippy)
    │
    │ Primary backups
    ▼
┌─────────────┐
│   Backups   │
└─────────────┘

Replica (Your server)
    │
    │ Secondary backups
    │ (for faster restore)
    ▼
┌─────────────┐
│   Backups   │
└─────────────┘

Schedule:
• Daily full backup (2 AM)
• Keep 7 days
• Test restore monthly
```

---

## 🚀 Future Enhancements

### Phase 2: Advanced Features

1. **Caching Layer**
   ```
   CDR API → Redis Cache → PostgreSQL
   • Cache frequently accessed data
   • Reduce database load
   • Sub-second responses
   ```

2. **Real-time Streaming**
   ```
   PostgreSQL → Kafka → WebSocket → Frontend
   • Live CDR updates
   • No polling needed
   • Real-time dashboards
   ```

3. **Data Warehouse**
   ```
   PostgreSQL → ETL → ClickHouse/TimescaleDB
   • Optimized for analytics
   • Faster aggregations
   • Historical analysis
   ```

---

## 📝 Summary

**Key Advantages:**
- ⚡ **10-50x faster** than XML API
- 🔒 **Zero impact** on production
- 📊 **Handles millions** of records
- 🔄 **Real-time sync** (< 1s lag)
- 💰 **Cost effective** (single server)
- 🛠️ **Easy to maintain**

**When to Use:**
- ✅ Historical CDR queries
- ✅ Large dataset exports
- ✅ Analytics and reporting
- ✅ Bulk operations

**When NOT to Use:**
- ❌ Real-time call control
- ❌ Writing CDR data
- ❌ Modifying production data

This architecture provides the best balance of performance, reliability, and cost for your CDR querying needs! 🎉

