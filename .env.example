# PostgreSQL Replica Connection
# Connection string format: postgres://user:password@host:port/database
# Password special chars: $ = %24, * = %2A, # = %23
DATABASE_URL=postgres://user:password@host:5432/database

# Or use individual components (DATABASE_URL takes precedence)
DB_HOST=your-postgres-host
DB_PORT=5432
DB_NAME=sippy
DB_USER=your-username
DB_PASSWORD=your-password
DB_MAX_CONNECTIONS=20

# API Configuration
API_PORT=3002
API_HOST=localhost

# CORS - Production domains
# Local: http://localhost:3000 for development
# Production: https://app.ovoky.io, https://ovoky.io
ALLOWED_ORIGINS=http://localhost:3000,https://app.ovoky.io,https://ovoky.io

# Authentication (use the same secret as your Next.js app)
# IMPORTANT: Generate a secure random string in production
# Generate with: openssl rand -base64 32
API_SECRET=your-secure-api-secret-here

# Production CDR API URL (for reference)
# This will be: https://cdrs.ovoky.io
